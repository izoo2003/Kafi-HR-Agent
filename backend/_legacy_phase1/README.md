# Legacy Phase-1 CV Ranking

Archived Gmail / Google Form pipeline from the first CV-ranking spike.
Kept for reference only — do not import from `app/`.

New work follows `docs/BACKEND_ARCHITECTURE.md` and feature packs under `docs/`.
