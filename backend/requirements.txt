# --- Core ---
python-dotenv==1.0.1
PyYAML==6.0.2
SQLAlchemy==2.0.51
pydantic==2.10.3
pydantic-settings==2.6.1
email-validator==2.2.0

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1

# --- API ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
python-multipart==0.0.17

# --- Migrations ---
alembic==1.14.0

# --- Postgres (Supabase) ---
psycopg2-binary==2.9.10
# Storage uses requests (already listed) against Supabase Storage HTTP API

# --- CV / reports (feature modules) ---
pdfplumber==0.11.4
python-docx==1.1.2
openpyxl==3.1.5
Pillow==10.4.0

# --- Optional LLM scoring (feature) ---
google-generativeai==0.8.3

# --- Timezones (Windows) ---
tzdata==2025.1

# --- Optional Google intake (legacy / future feature) ---
google-api-python-client==2.149.0
google-auth-httplib2==0.2.0
google-auth-oauthlib==1.2.1

# --- Microsoft Graph / Outlook CV intake ---
msal==1.31.1
requests>=2.33.0

# --- In-app KPI reminder scheduler (Asia/Karachi) ---
APScheduler==3.10.4
