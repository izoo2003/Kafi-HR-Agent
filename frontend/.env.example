# Local development — Vite proxies /api to the backend (port 8808).
VITE_API_BASE_URL=/api/v1

VITE_DEV_PORT=5288
VITE_API_PROXY_TARGET=http://127.0.0.1:8808

# Production / Vercel: use frontend/.env.production (Railway public API).
# VITE_API_BASE_URL=https://kafi-hr-agent.up.railway.app/api/v1
