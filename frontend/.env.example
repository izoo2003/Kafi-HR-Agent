# Leave empty in development — Vite proxies API paths to the backend.
VITE_API_BASE_URL=

# Dedicated frontend port for this HR Agent project (not 5173).
VITE_DEV_PORT=5288

# Backend this Vite proxy forwards to (must match backend API_PORT).
VITE_API_PROXY_TARGET=http://127.0.0.1:8808
